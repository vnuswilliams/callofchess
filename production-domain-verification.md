# Vérification du domaine de production

Le projet Vercel `lionchess` est rattaché au dépôt et accessible avec la session GitHub de l’utilisateur.

Le domaine `callofchess.vercel.app` a été ajouté avec le statut **Valid Configuration — Production**.

Le domaine historique `lionchess.vercel.app` reste actif avec une redirection **307 Temporary Redirect** vers `callofchess.vercel.app`, afin de préserver les anciens liens.

Le projet conserve aussi `echequier-plum.vercel.app` en Production. Les domaines `callofchess.online` et `www.callofchess.online` apparaissent en Invalid Configuration et ne font pas partie de la demande actuelle.
