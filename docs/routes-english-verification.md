# Vérification des routes anglaises

Les routes canoniques `/lesson/1`, `/account`, `/profile`, `/path` et `/ranking` ont été capturées sur le serveur de prévisualisation sans erreur de rendu.

L’ancien chemin `/classement` redirige vers `/ranking`, qui affiche la page bilingue « Global leaderboard ». L’ancien chemin `/lecon/2` redirige vers `/lesson/2` et conserve l’identifiant de la leçon : la page Development s’ouvre correctement.
